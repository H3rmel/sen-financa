import { Modal } from "../Index";

export const ModalRemoveTransaction = ({ id }) => {
  <Modal id="removeTransaction" title="Remover transação">
    <form method="dialog" className="flex justify-between flex-wrap mt-2">

    </form>
  </Modal>;
};
